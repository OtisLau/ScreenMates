import Foundation

// Streak tracking is not used in the core-func build.
// Kept as an empty stub so the file doesn't need to be removed from the Xcode project.
class StreakManager {
    static let shared = StreakManager()
    private init() {}
}
